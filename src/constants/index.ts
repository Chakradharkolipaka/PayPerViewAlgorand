// Central place for shared app constants.

export const APP_TAG = "PayPerViewAlgorand" as const;

/** Fixed payment amount in ALGO that viewers pay per view */
export const PAY_PER_VIEW_AMOUNT_ALGO = 3;

/** Maximum video file size in bytes (3 MB) */
export const MAX_VIDEO_SIZE_BYTES = 3 * 1024 * 1024;
